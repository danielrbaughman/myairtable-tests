# myAirtable Test Scenarios

Exhaustive test scenarios for the TypeScript output generated by myAirtable.

---

## 1. Initialization & Configuration

### 1.1 Airtable Main Class

- [ ] Construct `Airtable` with explicit `apiKey` and `baseId`
- [ ] Construct `Airtable` with only `apiKey` (baseId from env)
- [ ] Construct `Airtable` with only `baseId` (apiKey from env)
- [ ] Construct `Airtable` with no options (both from env)
- [ ] Construct `Airtable` when `AIRTABLE_API_KEY` env var is missing — should throw
- [ ] Construct `Airtable` when `AIRTABLE_BASE_ID` env var is missing — should throw
- [ ] Construct `Airtable` with invalid `baseId` (not matching `app[a-zA-Z0-9]{14}`) — should throw Zod validation error
- [ ] Construct `Airtable` with optional fields: `endpointUrl`, `apiVersion`, `requestTimeout`, `noRetryIfRateLimited`, `customHeaders`
- [ ] Verify `airtable.primary` is an instance of `PrimaryTable`
- [ ] Verify `airtable.secondary` is an instance of `SecondaryTable`
- [ ] Verify `airtable.table("Primary")` returns the same table as `airtable.primary`
- [ ] Verify `airtable.table("Secondary")` returns the same table as `airtable.secondary`

### 1.2 Config Registry (helpers.ts)

- [ ] `setAirtableConfig` registers config and sets default base ID
- [ ] `getApiKey` returns API key from registry when baseId matches
- [ ] `getApiKey` falls back to default base's config
- [ ] `getApiKey` falls back to `AIRTABLE_API_KEY` env var
- [ ] `getApiKey` throws when no key available
- [ ] `getBaseId` returns from registry default
- [ ] `getBaseId` falls back to `AIRTABLE_BASE_ID` env var
- [ ] `getBaseId` throws when neither available
- [ ] `getRequestTimeout` parses valid integer from env
- [ ] `getRequestTimeout` throws on non-numeric env value
- [ ] `getCustomHeaders` parses valid JSON from env
- [ ] `getCustomHeaders` throws on invalid JSON env value
- [ ] `getNoRetryIfRateLimited` parses "true"/"false" string from env

---

## 2. CRUD Operations — Table Level

### 2.1 Create

#### Single Record

- [x] Create a single `PrimaryModel` with only the primary key field set
- [x] Create a single `PrimaryModel` with all writable fields populated
- [x] Verify the returned model has a valid record ID (`rec[a-zA-Z0-9]{14}`)
- [x] Verify all set fields are returned with correct values
- [x] Verify computed fields (formulas, rollups, lookups, etc.) are populated on the returned model
- [x] Create using an Airtable.js `Record<FieldSet>` — verify returns `Record<FieldSet>`
- [x] Create using an `IRecord<FieldSet>` — verify returns `IRecord<FieldSet>`
- [ ] Create with `IRecord` using field names (not IDs) — verify field name-to-ID mapping works
- [ ] Create a record with no fields set (empty model) — verify API behavior
- [ ] Create a record setting a computed field (e.g., formula) — verify it is excluded from the API payload

#### Batch Create

- [ ] Create 2 records in a single batch call
- [ ] Create exactly 10 records (single batch boundary)
- [ ] Create 11 records (forces 2 batches: 10 + 1)
- [ ] Create 25 records (forces 3 batches: 10 + 10 + 5)
- [ ] Create 0 records (empty array) — should return empty array
- [ ] Verify all returned models have unique, valid record IDs
- [ ] Batch create with `IRecord[]` — verify correct return type
- [ ] Batch create with `Record<FieldSet>[]` — verify correct return type

#### Edge Cases

- [ ] Create a record with special characters in text fields (quotes, newlines, unicode, emoji)
- [ ] Create a record with very long text in `longText` field
- [ ] Create a record with rich text (markdown formatting) in `longTextWithRichText`
- [ ] Create a record with `checkbox` set to `true`
- [ ] Create a record with `checkbox` set to `false`
- [ ] Create a record with a `date` field (ISO string, date only)
- [ ] Create a record with a `dateWithTime` field (ISO string with time)
- [ ] Create a record with zero values for number fields (`numberInt: 0`, `numberFloat: 0`)
- [ ] Create a record with negative number values
- [ ] Create a record with very large number values
- [ ] Create a record with float precision edge cases (`0.1 + 0.2`)
- [ ] Create a record with `currencyFloat` and `currencyInt` values
- [ ] Create a record with `percentFloat` (e.g., `0.5` for 50%) and `percentInt`
- [ ] Create a record with `duration` in milliseconds
- [ ] Create a record with `rating` value (within valid range)
- [ ] Create a record with `rating` value of 0 or out of range
- [ ] Create a record with `email` field
- [ ] Create a record with `phoneNumber` field
- [ ] Create a record with `url` field
- [ ] Create a record with `barcode` field
- [ ] Create a record with `singleSelect` set to a valid option ("Choice 1")
- [ ] Create a record with `singleSelect` set to an invalid option — verify API error
- [ ] Create a record with `multipleSelect` set to one valid option
- [ ] Create a record with `multipleSelect` set to all valid options
- [ ] Create a record with `multipleSelect` containing an invalid option — verify API error
- [ ] Create a record with `multipleSelect` as empty array
- [ ] Create a record with linked records (`linkSingle` with one record ID)
- [ ] Create a record with linked records (`linkMultiple` with multiple record IDs)
- [ ] Create a record with linked record pointing to non-existent record — verify API error
- [ ] Create a record on the `Secondary` table

### 2.2 Read (Get)

#### Single Record by ID

- [ ] Get a record by valid ID — returns correct model
- [ ] Get a record by valid ID — verify all field values match
- [ ] Get a record by invalid ID (malformed) — should throw Zod validation error
- [ ] Get a record by non-existent ID (valid format) — verify behavior (empty model or error)
- [ ] Get a record with `returnAs: "model"` — returns `PrimaryModel` instance
- [ ] Get a record with `returnAs: "record"` — returns Airtable.js `Record<FieldSet>`
- [ ] Get a record with `returnAs: "interface"` — returns `IRecord<FieldSet>`
- [ ] Get a record with `fields` option — only specified fields returned
- [ ] Get a record with `useFieldIds: true` — field keys are IDs
- [ ] Get a record with `useFieldIds: false` — field keys are names
- [ ] Get a record with `onlyWritableFields: true` and `returnAs: "record"` — computed fields excluded
- [ ] Get a record with `onlyWritableFields: true` and `returnAs: "model"` — should have no effect (models always use field IDs)

#### Multiple Records by IDs

- [ ] Get multiple records by array of valid IDs
- [ ] Get 0 records (empty array) — should return empty array
- [ ] Get records by array with one invalid ID — should throw
- [ ] Get records by array with mix of existing and non-existent IDs — verify only existing returned
- [ ] Get records with `maxRecords` option — verify limit applied
- [ ] Get records with `pageSize` option
- [ ] Get records with `returnAs: "interface"` — all returned as `IRecord`

#### Query (No IDs)

- [ ] Get all records with no options — returns all records as models
- [ ] Get records with `formula` filter — returns only matching records
- [ ] Get records with `view` filter by view name ("Grid view")
- [ ] Get records with `view` filter by view ID
- [ ] Get records with `maxRecords: 1` — returns exactly 1 record
- [ ] Get records with `pageSize: 1` — paginates correctly, returns all
- [ ] Get records combining `formula` + `view` + `maxRecords`
- [ ] Get records with `returnAs: "model"` (default)
- [ ] Get records with `returnAs: "record"`
- [ ] Get records with `returnAs: "interface"`
- [ ] Get records with `fields` filter — only specified fields in response
- [ ] Get records from an empty table — returns empty array
- [ ] Get records with invalid formula string — verify API error is thrown

### 2.3 Update

#### Single Record

- [ ] Update a single field on a model, verify only dirty field sent
- [ ] Update multiple fields on a model
- [ ] Verify computed fields are not included in update payload
- [ ] Update and verify returned model has updated values
- [ ] Update using Airtable.js `Record<FieldSet>`
- [ ] Update using `IRecord<FieldSet>`
- [ ] Update with `IRecord` using field names — verify name-to-ID mapping
- [ ] Update a record that doesn't exist — verify API error
- [ ] Update a record with no changes (no dirty fields) — verify behavior (empty payload sent)

#### Batch Update

- [ ] Update 2 records in a batch
- [ ] Update exactly 10 records (single batch boundary)
- [ ] Update 11 records (forces 2 batches)
- [ ] Update 0 records (empty array) — returns empty array
- [ ] Batch update with mixed `IRecord[]`
- [ ] Batch update with mixed `Record<FieldSet>[]`

#### Field-Specific Updates

- [ ] Update `singleLineText` to a new string value
- [ ] Update `singleLineText` to empty string
- [ ] Update `singleLineText` to `undefined` (clear field)
- [ ] Update `checkbox` from true to false
- [ ] Update `checkbox` from false to true
- [ ] Update `numberInt` to a new value
- [ ] Update `numberInt` to 0
- [ ] Update `singleSelect` to a different valid option
- [ ] Update `singleSelect` to `undefined` (clear)
- [ ] Update `multipleSelect` to add an option
- [ ] Update `multipleSelect` to remove an option
- [ ] Update `multipleSelect` to empty array
- [ ] Update `date` field to new date string
- [ ] Update `dateWithTime` field
- [ ] Update linked record fields (change linked record IDs)
- [ ] Update attachment field

### 2.4 Upsert

- [ ] Upsert a new record (no existing ID) — should create
- [ ] Upsert an existing record (has valid ID) — should update
- [ ] Upsert a mix of new and existing records in batch
- [ ] Upsert with `IRecord` input
- [ ] Upsert with `Record<FieldSet>` input
- [ ] Upsert 0 records — returns empty array
- [ ] Upsert single new model — returns single model (not array)
- [ ] Upsert single existing model — returns single model (not array)
- [ ] Upsert where record has ID but record doesn't exist in Airtable — should create

### 2.5 Delete

#### Single Delete

- [ ] Delete a record by valid ID — verify record is gone
- [ ] Delete a record by invalid ID (malformed) — should throw Zod validation error
- [ ] Delete a non-existent record — verify API error

#### Batch Delete

- [ ] Delete 2 records by IDs
- [ ] Delete exactly 10 records (single batch boundary)
- [ ] Delete 11 records (forces 2 batches)
- [ ] Delete 0 records (empty array) — verify behavior
- [ ] Delete with array containing an invalid ID — should throw

---

## 3. Model Operations

### 3.1 Construction & Initialization

- [ ] Construct `PrimaryModel` with no args — verify defaults
- [ ] Construct `PrimaryModel` with partial data (only `primaryKey`)
- [ ] Construct `PrimaryModel` with full data (all fields)
- [ ] Construct `PrimaryModel` with invalid record ID — should throw Zod error
- [ ] Construct `PrimaryModel` with valid record ID — verify `id` is set
- [ ] Verify `tableName` returns "Primary"
- [ ] Verify `tableId` returns the correct table ID
- [ ] Verify newly constructed model has `_isNew = true`

### 3.2 Field Accessors (Getters/Setters)

#### Text Fields

- [ ] Get/set `primaryKey` — string type
- [ ] Get/set `singleLineText` — string type
- [ ] Get/set `longText` — string type, supports multiline
- [ ] Get/set `longTextWithRichText` — string type, supports rich text
- [ ] Get/set `email` — string type
- [ ] Get/set `phoneNumber` — string type
- [ ] Get/set `url` — string type
- [ ] Get/set `barcode` — string type

#### Number Fields

- [ ] Get/set `numberInt` — number type
- [ ] Get/set `numberFloat` — number type
- [ ] Get/set `currencyInt` — number type
- [ ] Get/set `currencyFloat` — number type
- [ ] Get/set `percentInt` — number type
- [ ] Get/set `percentFloat` — number type
- [ ] Get/set `duration` — number type (milliseconds)
- [ ] Get/set `autoNumber` — number type (read-only in practice)
- [ ] Get/set `rating` — any type

#### Boolean Fields

- [ ] Get/set `checkbox` — boolean type

#### Date Fields

- [ ] Get/set `date` — string type (ISO date)
- [ ] Get/set `dateWithTime` — string type (ISO datetime)

#### Select Fields

- [ ] Get/set `singleSelect` — typed to `PrimarySingleSelectOption`
- [ ] Get/set `multipleSelect` — typed to `PrimaryMultipleSelectOption[]`

#### Collaborator Fields

- [ ] Get/set `user` — `Collaborator` type (id, email, name)
- [ ] Get/set `userAllowMultiple` — `Collaborator[]` type

#### Computed Fields (Read-Only)

- [ ] Get `createdAtTime` — string (ISO datetime)
- [ ] Get `lastModifiedTime` — string (ISO datetime)
- [ ] Get `createdBy` — `Collaborator` type
- [ ] Get `lastModifiedBy` — `Collaborator` type
- [ ] Get `formulaId` — returns RECORD_ID() value
- [ ] Get `formulaSimple` — returns number (sum formula)
- [ ] Get `formulaComplex` — returns concatenated string
- [ ] Get `formulaNested` — returns nested formula result
- [ ] Get `lookup` — returns `string[]`
- [ ] Get `rollup` — returns `string[]`
- [ ] Get `button` — returns `AirtableButton` (label, url)

#### Attachment Fields

- [ ] Get/set `attachment` — `Attachment[]` type
- [ ] Set attachment with full attachment object (id, url, filename, size, type, thumbnails)
- [ ] Verify attachment sanitization on save (only id, url, filename, size, type sent)

#### Linked Record Fields

- [ ] Get `linkSingle` — returns `LinkedRecords<SecondaryModel>`
- [ ] Get `linkMultiple` — returns `LinkedRecords<SecondaryModel>`

### 3.3 Change Tracking

- [ ] Newly constructed model — `hasChanges()` returns true (new model), `getChangedFields()` returns all set fields
- [ ] Model from `fromRecord` — `hasChanges()` returns false, dirty flags cleared
- [ ] Set a field — `hasChanges()` returns true
- [ ] Set a field — `getChangedFields()` includes that field name
- [ ] Set multiple fields — `getChangedFields()` includes all changed field names
- [ ] After `save()` — dirty flags are cleared, `hasChanges()` returns false
- [ ] After `fetch()` — dirty flags are cleared, `hasChanges()` returns false
- [ ] Setting a linked record's `id` marks the parent field as dirty
- [ ] Setting a linked record via `.set()` marks the parent field as dirty

### 3.4 Serialization

#### toJson()

- [ ] `toJson()` returns plain object with all fields
- [ ] `toJson()` includes `id` when set
- [ ] `toJson()` returns `undefined` for `id` when empty string
- [ ] `toJson()` serializes linked records as arrays of IDs
- [ ] `toJson()` serializes single linked record as array of IDs

#### toRecord()

- [ ] `toRecord(false)` returns Record with field names as keys
- [ ] `toRecord(true)` returns Record with field IDs as keys
- [ ] `toRecord()` throws if record is not initialized

#### toIRecord()

- [ ] `toIRecord()` returns `{ id, fields }` interface
- [ ] `toIRecord(true)` uses field IDs
- [ ] `toIRecord(false)` uses field names

#### toCreateRecordData()

- [ ] Returns `{ fields }` without `id`
- [ ] Only includes writable fields
- [ ] Excludes computed fields
- [ ] On a new model, includes all set fields
- [ ] Correctly serializes linked records as ID arrays
- [ ] Correctly sanitizes attachments

#### toUpdateRecordData()

- [ ] Returns `{ id, fields }` with record ID
- [ ] Throws if `id` is not set
- [ ] Only includes dirty (modified) fields on non-new models
- [ ] Includes all fields on new models

### 3.5 Instance Methods

#### save()

- [ ] Save a new model (no ID) — creates record, returns with ID
- [ ] Save an existing model (has ID) — updates record
- [ ] Save validates via Zod schema before saving
- [ ] Save clears dirty flags after success
- [ ] Save updates model with returned data from API
- [ ] Save throws on API error (e.g., invalid data)
- [ ] Save throws if record is not initialized

#### fetch()

- [ ] Fetch refreshes model from server
- [ ] Fetch overwrites local unsaved changes
- [ ] Fetch clears dirty flags
- [ ] Fetch throws if record is not initialized
- [ ] Fetch throws on API error (e.g., deleted record)

#### delete() (instance)

- [ ] Delete removes the record from server
- [ ] After delete, `id` is empty string
- [ ] After delete, `record` is undefined
- [ ] Delete throws if record is not initialized
- [ ] Delete throws on API error

#### validate()

- [ ] Validate passes for a valid model
- [ ] Validate throws `ZodError` for invalid data
- [ ] Validate is a no-op if schema is not defined

### 3.6 Static Factory Methods

#### fromRecord()

- [ ] Creates model from Airtable.js Record
- [ ] Sets all field values from the record
- [ ] Clears dirty flags (model is not "new")
- [ ] Accepts optional config (baseId, apiKey)
- [ ] Config is used for subsequent save/fetch calls

#### fromId()

- [ ] Creates model with just an ID (no data fetched)
- [ ] Accepts optional config
- [ ] Subsequent `fetch()` loads the data

### 3.7 Field Access by Name

- [ ] `model.get("Primary Key")` returns primaryKey value
- [ ] `model.set("Primary Key", "new value")` updates primaryKey
- [ ] `model.get("Invalid Field Name")` throws error
- [ ] `model.set("Invalid Field Name", "value")` throws error
- [ ] `model.get()` / `model.set()` throw if record is not initialized

---

## 4. Linked Records

### 4.1 LinkedRecords (Multiple)

- [ ] Constructor with array of record IDs — `ids` returns the array
- [ ] Constructor with empty array — `ids` returns empty array
- [ ] Constructor with `undefined` — `ids` returns undefined
- [ ] `get()` fetches all linked records, returns `SecondaryModel[]`
- [ ] `get()` caches results — second call doesn't re-fetch
- [ ] `get(true)` forces re-fetch even if cached
- [ ] `set(models)` updates IDs from model instances
- [ ] `set([])` clears records and IDs
- [ ] `set(undefined)` clears records and IDs
- [ ] Setting `ids` directly triggers `onDirty` callback
- [ ] Setting via `set()` triggers `onDirty` callback
- [ ] Linked records with invalid record IDs in constructor — should throw Zod error

### 4.2 LinkedRecord (Single — if applicable)

- [ ] Constructor with record ID — `id` returns the ID
- [ ] Constructor with `undefined` — `id` returns undefined
- [ ] `get()` fetches the linked record
- [ ] `get()` caches result
- [ ] `get(true)` forces re-fetch
- [ ] `set(model)` updates ID from model instance
- [ ] `set(undefined)` clears record and ID
- [ ] Setting `id` directly triggers `onDirty` callback

### 4.3 End-to-End Linked Record Operations

- [ ] Create a Secondary record, then create a Primary record linked to it
- [ ] Read a Primary record and traverse `linkSingle` to get the linked Secondary record
- [ ] Read a Primary record and traverse `linkMultiple` to get all linked Secondary records
- [ ] Update a Primary record's `linkSingle` to point to a different Secondary record
- [ ] Update a Primary record's `linkMultiple` to add a linked record
- [ ] Update a Primary record's `linkMultiple` to remove a linked record
- [ ] Clear a Primary record's linked records (set to empty)
- [ ] Verify `lookup` field reflects linked record data after linking
- [ ] Verify `rollup` field reflects linked record data after linking
- [ ] Delete a linked Secondary record and verify behavior on the Primary side

---

## 5. Formula Builders

### 5.1 Logic Operators

- [ ] `AND(a, b)` produces `AND(a,b)`
- [ ] `AND(a, b, c)` produces `AND(a,b,c)`
- [ ] `AND()` with empty strings filtered out
- [ ] `OR(a, b)` produces `OR(a,b)`
- [ ] `OR()` with empty strings filtered out
- [ ] `XOR(a, b)` produces `XOR(a,b)`
- [ ] `NOT(a)` produces `NOT(a)`
- [ ] Nested: `AND(OR(a, b), NOT(c))`

### 5.2 ID Formulas

- [ ] `PrimaryModel.f.id.equals("recXXXXXXXXXXXXXX")` — valid ID
- [ ] `PrimaryModel.f.id.equals("invalid")` — throws Zod validation error
- [ ] `PrimaryModel.f.id.inList(["recA", "recB"])` — produces `OR(RECORD_ID()='recA',RECORD_ID()='recB')`
- [ ] `PrimaryModel.f.id.inList([])` — produces `FALSE()`
- [ ] `PrimaryModel.f.id.inList(["recA"])` — produces `RECORD_ID()='recA'` (no OR wrapper)

### 5.3 TextField Formulas

- [ ] `equals(value)` — case sensitive, no trim (default)
- [ ] `equals(value, false)` — case insensitive (uses LOWER)
- [ ] `equals(value, true, true)` — case sensitive with trim
- [ ] `equals(value, false, true)` — case insensitive with trim
- [ ] `equals` with quotes in value — properly escaped
- [ ] `notEquals(value)`
- [ ] `contains(value)` — default: case insensitive, trim
- [ ] `contains(value, true)` — case sensitive
- [ ] `contains(value, false, false)` — no trim
- [ ] `containsAny(["a", "b"])` — produces `OR(FIND(...)>0, FIND(...)>0)`
- [ ] `containsAll(["a", "b"])` — produces `AND(FIND(...)>0, FIND(...)>0)`
- [ ] `notContains(value)` — wraps in `NOT(...)`
- [ ] `startsWith(value)` — `FIND(...)=1`
- [ ] `notStartsWith(value)` — `FIND(...)!=1`
- [ ] `endsWith(value)` — uses LEN calculations
- [ ] `notEndsWith(value)`
- [ ] `regexMatch(pattern)` — uses `REGEX()` function
- [ ] `empty()` — `{field}=BLANK()`
- [ ] `notEmpty()` — `{field}`
- [ ] `phoneEquals(value)` — normalizes phone number (strips spaces, dashes, parens, +, dots)

### 5.4 NumberField Formulas

- [ ] `equals(value)` — `{field}=value`
- [ ] `notEquals(value)` — `{field}!=value`
- [ ] `greaterThan(value)` — `{field}>value`
- [ ] `lessThan(value)` — `{field}<value`
- [ ] `greaterThanOrEquals(value)` — `{field}>=value`
- [ ] `lessThanOrEquals(value)` — `{field}<=value`
- [ ] `between(min, max)` — inclusive (default): `AND({field}>=min,{field}<=max)`
- [ ] `between(min, max, false)` — exclusive: `AND({field}>min,{field}<max)`
- [ ] `empty()` — inherited from Field
- [ ] `notEmpty()` — inherited from Field
- [ ] Passing non-number value — should throw Zod validation error

### 5.5 BooleanField Formulas

- [ ] `true()` — `{field}=TRUE()`
- [ ] `false()` — `{field}=FALSE()`
- [ ] `equals(true)` — `{field}=TRUE()`
- [ ] `equals(false)` — `{field}=FALSE()`
- [ ] Passing non-boolean — should throw Zod validation error

### 5.6 DateField Formulas

- [ ] `on(date)` — equality comparison with DATETIME_PARSE
- [ ] `on()` — returns DateComparison for chaining
- [ ] `before(date)` — less than comparison
- [ ] `after(date)` — greater than comparison
- [ ] `onOrBefore(date)` — less than or equal
- [ ] `onOrAfter(date)` — greater than or equal
- [ ] `notOn(date)` — not equal
- [ ] `between(startDate, endDate)` — inclusive (default)
- [ ] `between(startDate, endDate, false)` — exclusive
- [ ] Date from `Date` object
- [ ] Date from ISO string
- [ ] Invalid date string — should throw error
- [ ] DateComparison chaining: `.on().daysAgo(7)` — relative time
- [ ] `.on().hoursAgo(24)` — hours
- [ ] `.on().weeksAgo(1)` — weeks
- [ ] `.on().monthsAgo(3)` — months
- [ ] `.on().yearsAgo(1)` — years
- [ ] `.on().minutesAgo(30)` — minutes
- [ ] `.on().secondsAgo(60)` — seconds
- [ ] `.on().millisecondsAgo(1000)` — milliseconds
- [ ] `.on().quartersAgo(2)` — quarters
- [ ] `.after().daysAgo(7)` — "after 7 days ago" = within last 7 days
- [ ] `.before().daysAgo(30)` — "before 30 days ago" = older than 30 days

### 5.7 SingleSelectField Formulas

- [ ] `equals(value)` — typed to valid options (`"Choice 1"`)
- [ ] `notEquals(value)` — typed to valid options
- [ ] Inherits all TextField methods (contains, startsWith, etc.)

### 5.8 MultiSelectField Formulas

- [ ] `containsOption(value)` — typed to valid options
- [ ] `containsAllOptions(values)` — all must be present
- [ ] `containsAnyOptions(values)` — any must be present
- [ ] `notContainsOption(value)` — does not contain
- [ ] `notContainsOptions(values)` — does not contain any
- [ ] Inherits all TextField and SingleSelectField methods

### 5.9 AttachmentsField Formulas

- [ ] `notEmpty()` — `LEN({field})>0`
- [ ] `empty()` — `LEN({field})=0`
- [ ] `count(n)` — `LEN({field})=n`

### 5.10 Formula Integration with Queries

- [ ] Use a text formula in `get({ formula })` and verify it filters correctly
- [ ] Use a number formula in `get({ formula })` and verify filtering
- [ ] Use a boolean formula in `get({ formula })`
- [ ] Use a date formula in `get({ formula })`
- [ ] Use a combined `AND`/`OR` formula in `get({ formula })`
- [ ] Use `PrimaryModel.f.id.equals(id)` as a formula to fetch by ID
- [ ] Use `PrimaryModel.f.id.inList(ids)` as a formula to fetch multiple by IDs
- [ ] Use a formula with select field filtering

---

## 6. Zod Validation & Schemas

### 6.1 Record ID Validation

- [ ] `recordIdSchema` accepts valid ID: `recXXXXXXXXXXXXXX` (rec + 14 alphanumeric)
- [ ] `recordIdSchema` rejects: empty string
- [ ] `recordIdSchema` rejects: `rec` (too short)
- [ ] `recordIdSchema` rejects: `recABCDEFGHIJKLMNO` (too long — 15 chars)
- [ ] `recordIdSchema` rejects: `tblXXXXXXXXXXXXXX` (wrong prefix)
- [ ] `recordIdSchema` rejects: string with special characters
- [ ] `validateRecordIds` with single valid ID — passes
- [ ] `validateRecordIds` with array of valid IDs — passes
- [ ] `validateRecordIds` with single invalid ID — throws
- [ ] `validateRecordIds` with array containing one invalid ID — throws

### 6.2 Base ID Validation

- [ ] `baseIdSchema` accepts valid ID: `appXXXXXXXXXXXXXX` (app + 14 alphanumeric)
- [ ] `baseIdSchema` rejects: empty string
- [ ] `baseIdSchema` rejects: wrong prefix
- [ ] `baseIdSchema` rejects: wrong length

### 6.3 PrimarySchema

- [ ] Validates a fully populated valid object
- [ ] Validates an empty object (all fields optional)
- [ ] Validates with only `id` set
- [ ] Rejects invalid `id` format
- [ ] Validates `attachment` as array of `AirtableAttachmentSchema`
- [ ] Validates `checkbox` as boolean
- [ ] Rejects `checkbox` as non-boolean
- [ ] Validates `singleSelect` as one of `PrimarySingleSelectOptions`
- [ ] Rejects `singleSelect` with invalid option
- [ ] Validates `multipleSelect` as array of `PrimaryMultipleSelectOptions`
- [ ] Rejects `multipleSelect` with invalid option in array
- [ ] Validates `linkSingle` and `linkMultiple` as arrays of record IDs
- [ ] Rejects `linkSingle` with invalid record ID in array
- [ ] Validates `user` as `AirtableCollaboratorSchema`
- [ ] Validates `userAllowMultiple` as array of `AirtableCollaboratorSchema`
- [ ] Validates formula fields with union types (value | SpecialNumber | ErrorValue)
- [ ] Validates `button` as union of `AirtableButtonSchema | ErrorValueSchema`
- [ ] Validates `createdBy` as union of `AirtableCollaboratorSchema | ErrorValueSchema`
- [ ] Validates computed fields with `ErrorValueSchema` variant (e.g., `{ error: "#ERROR" }`)
- [ ] Validates computed fields with `SpecialNumberSchema` variant (e.g., `{ specialValue: "NaN" }`)

### 6.4 Special Type Schemas

- [ ] `AirtableAttachmentSchema` validates full attachment (id, url, filename, size, type, thumbnails)
- [ ] `AirtableAttachmentSchema` validates minimal attachment (all optional)
- [ ] `AirtableThumbnailSchema` validates with url, width, height
- [ ] `AirtableCollaboratorSchema` validates with id, email, name
- [ ] `AirtableButtonSchema` validates with label, url
- [ ] `SpecialNumberSchema` validates `{ specialValue: "NaN" }`
- [ ] `ErrorValueSchema` validates `{ error: "#ERROR!" }`

---

## 7. Generated Types & Mappings

### 7.1 Field Name/ID/Property Mappings

- [ ] `PrimaryFieldNameIdMapping` — every field name maps to correct field ID
- [ ] `PrimaryFieldIdNameMapping` — every field ID maps back to correct field name
- [ ] `PrimaryFieldNamePropertyMapping` — every field name maps to correct property name
- [ ] `PrimaryFieldPropertyNameMapping` — every property maps back to correct field name
- [ ] `PrimaryFieldIdPropertyMapping` — every field ID maps to correct property
- [ ] `PrimaryFieldPropertyIdMapping` — every property maps to correct field ID
- [ ] All mappings are bidirectional and consistent (A->B->A roundtrip)

### 7.2 View Mappings

- [ ] `PrimaryViewNameIdMapping` maps "Grid view" to correct view ID
- [ ] `PrimaryViewIdNameMapping` maps view ID back to "Grid view"

### 7.3 Field Lists

- [ ] `PrimaryFields` contains all 37 field names
- [ ] `PrimaryFieldIds` contains all 37 field IDs
- [ ] `PrimaryFieldPropertys` contains all 37 property names
- [ ] `PrimaryCalculatedFields` contains exactly the computed field names
- [ ] `PrimaryCalculatedFieldIds` contains exactly the computed field IDs
- [ ] `PrimaryWritableFields` contains only non-computed field names
- [ ] `PrimaryWritableFieldIds` contains only non-computed field IDs
- [ ] Writable + Calculated = All fields

### 7.4 Select Options

- [ ] `PrimarySingleSelectOptions` contains `["Choice 1", "Choice 2", "Choice 3"]`
- [ ] `PrimaryMultipleSelectOptions` contains `["Option 1", "Option 2", "Option 3"]`
- [ ] `PrimarySingleSelectOption` type is union of the three choices
- [ ] `PrimaryMultipleSelectOption` type is union of the three options

### 7.5 FieldSet Interfaces

- [ ] `PrimaryFieldSet` has correct types for each field name key
- [ ] `PrimaryFieldSetIds` has correct types for each field ID key
- [ ] Both interfaces extend `FieldSet`

---

## 8. Field Type Round-Trip Tests

For each writable field, create a record with a known value, read it back, and verify the value is correct.

### 8.1 String Fields

- [ ] `singleLineText`: write "hello" → read back "hello"
- [ ] `longText`: write multiline string → read back identical
- [ ] `longTextWithRichText`: write markdown string → read back identical
- [ ] `email`: write "test@example.com" → read back
- [ ] `phoneNumber`: write "+1-555-0100" → read back
- [ ] `url`: write "https://example.com" → read back
- [ ] `barcode`: write "1234567890" → read back

### 8.2 Number Fields

- [ ] `numberInt`: write 42 → read back 42
- [ ] `numberFloat`: write 3.14 → read back 3.14
- [ ] `currencyInt`: write 100 → read back 100
- [ ] `currencyFloat`: write 99.99 → read back 99.99
- [ ] `percentInt`: write 0.5 → read back 0.5
- [ ] `percentFloat`: write 0.123 → read back 0.123
- [ ] `duration`: write 3600 → read back 3600
- [ ] `rating`: write 3 → read back 3

### 8.3 Boolean Fields

- [ ] `checkbox`: write true → read back true
- [ ] `checkbox`: write false → read back undefined/false (Airtable omits false)

### 8.4 Date Fields

- [ ] `date`: write "2025-01-15" → read back "2025-01-15"
- [ ] `dateWithTime`: write "2025-01-15T10:30:00.000Z" → read back with same datetime

### 8.5 Select Fields

- [ ] `singleSelect`: write "Choice 1" → read back "Choice 1"
- [ ] `multipleSelect`: write ["Option 1", "Option 2"] → read back ["Option 1", "Option 2"]

### 8.6 Linked Records

- [ ] `linkSingle`: link one record → read back array with one ID
- [ ] `linkMultiple`: link three records → read back array with three IDs

### 8.7 Computed Fields (Read-Only)

- [ ] `formulaId`: equals the record's own ID
- [ ] `formulaSimple`: equals `numberInt + numberFloat`
- [ ] `createdAtTime`: is a valid ISO datetime string
- [ ] `lastModifiedTime`: is a valid ISO datetime string and >= createdAtTime
- [ ] `createdBy`: has valid collaborator shape
- [ ] `lastModifiedBy`: has valid collaborator shape
- [ ] `autoNumber`: is a positive integer

---

## 9. Error Handling

### 9.1 API Errors

- [ ] Invalid API key — throws meaningful error
- [ ] Invalid base ID (valid format but non-existent) — throws API error
- [ ] Rate limiting (429) — verify retry behavior or error surfacing
- [ ] Network timeout — verify error surfaces properly
- [ ] Creating a record with invalid field data — API error thrown
- [ ] Updating a non-existent record — API error thrown
- [ ] Deleting a non-existent record — API error thrown

### 9.2 Validation Errors

- [ ] Invalid record ID in `get()` — Zod error
- [ ] Invalid record ID in `delete()` — Zod error
- [ ] Invalid record ID in `LinkedRecord` constructor — Zod error
- [ ] Invalid record ID in `LinkedRecords` constructor — Zod error
- [ ] Invalid base ID in `AirtableTable` constructor — Zod error
- [ ] Invalid formula input (non-string to TextField.equals) — Zod error
- [ ] Invalid formula input (non-number to NumberField.equals) — Zod error
- [ ] Invalid formula input (non-boolean to BooleanField.equals) — Zod error

### 9.3 Model State Errors

- [ ] Calling `toRecord()` on model without initialized record — throws
- [ ] Calling `save()` on model without initialized record — throws
- [ ] Calling `fetch()` on model without initialized record — throws
- [ ] Calling `delete()` on model without initialized record — throws
- [ ] Calling `toUpdateRecordData()` on model without ID — throws
- [ ] Calling `model.get("fieldName")` without initialized record — throws
- [ ] Calling `model.set("fieldName", value)` without initialized record — throws

---

## 10. Field Name/ID Mapping in I/O

### 10.1 Field Names in Records/Interfaces

- [ ] Create with `IRecord` using field names (e.g., `"Primary Key"`) — correctly maps to field IDs for API
- [ ] Create with `IRecord` using field IDs (e.g., `"fldol5Q4wmQJQvPRy"`) — sent as-is
- [ ] Update with `IRecord` using field names — correctly mapped
- [ ] Read with `returnAs: "record"` and `useFieldIds: false` — keys are field names
- [ ] Read with `returnAs: "record"` and `useFieldIds: true` — keys are field IDs
- [ ] Read with `returnAs: "model"` — always uses field IDs internally
- [ ] Read with `returnAs: "interface"` — keys match query's useFieldIds setting

### 10.2 Writable Field Filtering

- [ ] When creating via `IRecord`, computed fields are stripped (only writable fields sent)
- [ ] When updating via `IRecord`, computed fields are stripped
- [ ] `onlyWritableFields` option restricts API response to writable fields
- [ ] Model's `toCreateRecordData()` never includes computed fields
- [ ] Model's `toUpdateRecordData()` never includes computed fields

---

## 11. Concurrency & Batch Boundaries

- [ ] Batch create: exactly 10 records = 1 API call
- [ ] Batch create: 11 records = 2 API calls (10 + 1)
- [ ] Batch create: 20 records = 2 API calls (10 + 10)
- [ ] Batch update: same boundaries as create
- [ ] Batch delete: same boundaries (groups of 10)
- [ ] Upsert with mix of creates and updates — both batched correctly
- [ ] Upsert triggers parallel create + update via `Promise.all`
- [ ] Large batch (100 records) — all created successfully in 10 batches

---

## 12. Return Type Polymorphism

- [ ] `get(id)` returns `PrimaryModel` by default
- [ ] `get(id, { returnAs: "model" })` returns `PrimaryModel`
- [ ] `get(id, { returnAs: "record" })` returns `Record<PrimaryFieldSet>`
- [ ] `get(id, { returnAs: "interface" })` returns `IRecord<PrimaryFieldSet>`
- [ ] `get([ids])` returns `PrimaryModel[]` by default
- [ ] `get([ids], { returnAs: "record" })` returns `Record<PrimaryFieldSet>[]`
- [ ] `get([ids], { returnAs: "interface" })` returns `IRecord<PrimaryFieldSet>[]`
- [ ] `get({})` returns `PrimaryModel[]` by default
- [ ] `get({ returnAs: "record" })` returns `Record<PrimaryFieldSet>[]`
- [ ] `get({ returnAs: "interface" })` returns `IRecord<PrimaryFieldSet>[]`
- [ ] `create(model)` returns `PrimaryModel`
- [ ] `create(record)` returns `Record<PrimaryFieldSet>`
- [ ] `create(irecord)` returns `IRecord<PrimaryFieldSet>`
- [ ] `update(model)` returns `PrimaryModel`
- [ ] `update(record)` returns `Record<PrimaryFieldSet>`
- [ ] `update(irecord)` returns `IRecord<PrimaryFieldSet>`
- [ ] `upsert(model)` returns `PrimaryModel`
- [ ] `upsert(record)` returns `Record<PrimaryFieldSet>`
- [ ] `upsert(irecord)` returns `IRecord<PrimaryFieldSet>`

---

## 13. View Filtering

- [ ] `get({ view: "Grid view" })` — resolves view name to view ID
- [ ] Verify view ID is sent to Airtable API
- [ ] Query with non-existent view name — passes through as-is (fallback behavior)

---

## 14. Cross-Table Operations

- [ ] Create records in both Primary and Secondary tables
- [ ] Link Primary record to Secondary record
- [ ] Read Primary record, traverse link to get Secondary data
- [ ] Verify lookup field on Primary shows data from linked Secondary record
- [ ] Verify rollup field on Primary aggregates linked Secondary data
- [ ] Update linked Secondary record, re-fetch Primary, verify lookup/rollup updated
- [ ] Unlink records, verify lookup/rollup clear

---

## 15. Edge Cases & Boundary Conditions

### 15.1 Empty/Null/Undefined Values

- [ ] Create record with all fields `undefined`
- [ ] Create record with all fields set to `null` — verify behavior
- [ ] Read record where optional fields are not set — returns `undefined`
- [ ] Update field to `undefined` to clear it
- [ ] Empty string vs `undefined` for text fields

### 15.2 Special Characters

- [ ] Text fields with double quotes
- [ ] Text fields with single quotes
- [ ] Text fields with backslashes
- [ ] Text fields with newlines (`\n`)
- [ ] Text fields with tabs (`\t`)
- [ ] Text fields with unicode characters
- [ ] Text fields with emoji
- [ ] Text fields with HTML-like content (`<script>`)
- [ ] Formula builder with special characters in search values

### 15.3 Large Data

- [ ] Create and read back a record with maximum-length text
- [ ] Batch operation with maximum allowed records
- [ ] Multiple select with all options selected
- [ ] Multiple linked records (many links)

### 15.4 Type Coercion

- [ ] Number field receiving a string from API — verify model handles it
- [ ] Boolean field receiving `undefined` (Airtable returns nothing for unchecked) — verify model treats as false/undefined
- [ ] Date field receiving various ISO format variations

---

## 16. Generated Formula Namespace

- [ ] `PrimaryFormulas.id` is an `ID` instance
- [ ] `PrimaryFormulas.primaryKey` is a `TextField` instance
- [ ] `PrimaryFormulas.numberInt` is a `NumberField` instance
- [ ] `PrimaryFormulas.checkbox` is a `BooleanField` instance
- [ ] `PrimaryFormulas.date` is a `DateField` instance
- [ ] `PrimaryFormulas.singleSelect` is a `SingleSelectField<PrimarySingleSelectOption>` instance
- [ ] `PrimaryFormulas.multipleSelect` is a `MultiSelectField<PrimaryMultipleSelectOption>` instance
- [ ] `PrimaryFormulas.attachment` is an `AttachmentsField` instance
- [ ] `PrimaryFormulas.currencyFloat` is a `NumberField` instance
- [ ] `PrimaryFormulas.duration` is a `NumberField` instance
- [ ] All formula fields use field IDs (not names) internally
- [ ] `PrimaryModel.f` is an alias for `PrimaryFormulas`

---

## 17. Computed Field Behavior

- [ ] Computed fields are marked with `isComputed: true` in field descriptors
- [ ] Computed fields are excluded from `writableFields()` output
- [ ] Computed fields are excluded from create payloads
- [ ] Computed fields are excluded from update payloads (dirty or not)
- [ ] Computed fields are still readable on the model
- [ ] Setting a computed field's setter still marks it dirty but does not affect API calls
- [ ] `PrimaryCalculatedFields` matches the set of `isComputed: true` fields
- [ ] `PrimaryWritableFields` matches the set of `isComputed: false` fields

---

## 18. Input Type Detection

- [ ] `detectInputType` correctly identifies `PrimaryModel` instance as "model"
- [ ] `detectInputType` correctly identifies Airtable.js Record (has `.save()`) as "record"
- [ ] `detectInputType` correctly identifies plain object `{ id, fields }` as "interface"
- [ ] Mixed arrays (models + interfaces) — not supported, first item determines type
